# API Key Requests

The agent writes here when it needs an API key to implement a feature.
**You (the owner) check this file and add keys to `backend/.env`.**

---

_No requests yet. The agent will add entries here as it builds features._
