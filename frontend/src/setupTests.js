// Jest setup file — runs before every test.
// Note: @testing-library/jest-dom is not imported here because OneDrive sync
// blocks npm from writing to node_modules/@testing-library on this machine.
// Tests use standard Jest matchers (toBeTruthy, toEqual, etc.) instead.
